// const data = require('./data');
// // const printElements = require('./printElements');
// // const progressBar = require('./progressBar');

// const element = [];
// let allElements = [];

// const showElements = (e) => {
//   allElements = data.getElements();
//   const choosenElement = e.target;
//   choosenElement.setAttribute('disabled', disabled);
//   allElements.forEach((elements) => {
//     if (elements.id === choosenElement.id && element.indexOf(element) === -1) {
//       element.push(element);
//     };
//   });
//   data.setTotal(element);
//   printElementsToDom.printElements(element);
//   progressBar(element);
// };

// const itemsChecked = () => {
//   const checkbox = document.getElementsByClassName('element');
//   for (let i = 0; i < checkbox.length; i++) {
//     console.log('checkbox', checkbox);
//   }
// };

// module.exports = {
//   itemsChecked,
//   showElements,
// };
